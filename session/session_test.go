package session

import (
	"testing"
)

func TestSession(t *testing.T) {
	t.Run("TestGetSession", func(t *testing.T) {
		// store := sessions.NewCookieStore([]byte(strings.Random(10)))

		// session, err := store.Get(nil, "session")

		// if err != nil {
		// 	// failed
		// 	return
		// }

		// err = session.Save(nil, nil)

	})
}
